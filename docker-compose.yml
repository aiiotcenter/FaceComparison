services:
  face-comparison:
    build: .
    ports:
      - "9833:9833"
    volumes:
      - .:/app           # mount your source code
      - /app/node_modules  # but keep the container’s node_modules intact
    command: node index.js
